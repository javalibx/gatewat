server:
  port: 8090

spring:
  application:
    name: gateway
  profiles:
    active: dev
  cloud:
    gateway:
      routes:
        - id: biz-auth
          uri: lb://biz-auth
          predicates:
            - Path=/auth/**